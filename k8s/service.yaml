apiVersion: v1
kind: Service
metadata:
  name: healthcare-backend-service
spec:
  type: NodePort
  selector:
    app: healthcare-backend
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
